---
phase: 04-notifications-agent-capabilities
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - ui/src/api/webhooks.ts
  - ui/src/api/skillProfiles.ts
  - ui/src/api/issues.ts
  - ui/src/api/index.ts
  - ui/src/lib/queryKeys.ts
  - ui/src/components/WebhookEndpointList.tsx
  - ui/src/components/WebhookDeliveryLog.tsx
  - ui/src/components/SubtaskTree.tsx
  - ui/src/components/SkillProfileSelector.tsx
  - ui/src/pages/CompanySettings.tsx
  - ui/src/pages/IssueDetail.tsx
  - ui/src/pages/AgentDetail.tsx
autonomous: false
requirements:
  - NOTF-01
  - AGNT-01
  - AGNT-02

must_haves:
  truths:
    - "Operator can add/edit/delete webhook endpoints in Company Settings"
    - "Operator can view delivery history per webhook endpoint"
    - "Operator can see subtask tree with dependency arrows on issue detail page"
    - "Operator can select a skill profile for an agent in agent settings"
    - "Webhook endpoints show enabled/disabled status with event type subscriptions"
  artifacts:
    - path: "ui/src/components/WebhookEndpointList.tsx"
      provides: "Webhook management CRUD list component"
      min_lines: 50
    - path: "ui/src/components/WebhookDeliveryLog.tsx"
      provides: "Delivery history table per endpoint"
      min_lines: 30
    - path: "ui/src/components/SubtaskTree.tsx"
      provides: "Expandable subtask tree with dependency visualization"
      min_lines: 50
    - path: "ui/src/components/SkillProfileSelector.tsx"
      provides: "Dropdown for agent skill profile selection"
      min_lines: 30
  key_links:
    - from: "ui/src/components/WebhookEndpointList.tsx"
      to: "ui/src/api/webhooks.ts"
      via: "TanStack Query hooks for webhook CRUD"
      pattern: "webhooksApi"
    - from: "ui/src/components/SubtaskTree.tsx"
      to: "ui/src/api/issues.ts"
      via: "Subtask listing and dependency queries"
      pattern: "issuesApi.*subtasks"
    - from: "ui/src/components/SkillProfileSelector.tsx"
      to: "ui/src/api/skillProfiles.ts"
      via: "Profile list query"
      pattern: "skillProfilesApi"
    - from: "ui/src/pages/CompanySettings.tsx"
      to: "ui/src/components/WebhookEndpointList.tsx"
      via: "Webhooks section in settings"
      pattern: "WebhookEndpointList"
---

<objective>
Build the UI for webhook management, subtask visualization, and skill profile selection. Webhook config lives in Company Settings, subtask tree view appears on issue detail, and skill profile selector appears in agent settings.

Purpose: All Phase 4 backend features need operator-facing UI. Webhook management allows configuring endpoints and viewing delivery history. Subtask tree shows dependency relationships. Skill profile selector enables agent persona configuration.

Output: Webhook CRUD list + delivery log in Company Settings, expandable subtask tree on issue detail, skill profile dropdown on agent detail/settings.
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notifications-agent-capabilities/04-CONTEXT.md

@ui/src/pages/CompanySettings.tsx
@ui/src/pages/IssueDetail.tsx
@ui/src/pages/AgentDetail.tsx
@ui/src/api/client.ts
@ui/src/api/index.ts
@ui/src/lib/queryKeys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API clients, webhook management UI, and delivery log</name>
  <files>
    ui/src/api/webhooks.ts,
    ui/src/api/skillProfiles.ts,
    ui/src/api/issues.ts,
    ui/src/api/index.ts,
    ui/src/lib/queryKeys.ts,
    ui/src/components/WebhookEndpointList.tsx,
    ui/src/components/WebhookDeliveryLog.tsx,
    ui/src/pages/CompanySettings.tsx
  </files>
  <action>
    1. Create `ui/src/api/webhooks.ts`:
       - Using the existing `api` fetch wrapper from `ui/src/api/client.ts`
       - `list(companyId)`: GET /companies/:companyId/webhooks
       - `create(companyId, data)`: POST /companies/:companyId/webhooks
       - `update(companyId, webhookId, data)`: PATCH /companies/:companyId/webhooks/:webhookId
       - `remove(companyId, webhookId)`: DELETE /companies/:companyId/webhooks/:webhookId
       - `listDeliveries(companyId, webhookId)`: GET /companies/:companyId/webhooks/:webhookId/deliveries
       - `testDelivery(companyId, webhookId)`: POST /companies/:companyId/webhooks/:webhookId/test

    2. Create `ui/src/api/skillProfiles.ts`:
       - `list(companyId)`: GET /companies/:companyId/skill-profiles
       - `create(companyId, data)`: POST /companies/:companyId/skill-profiles
       - `update(companyId, profileId, data)`: PATCH /companies/:companyId/skill-profiles/:profileId
       - `remove(companyId, profileId)`: DELETE /companies/:companyId/skill-profiles/:profileId
       - `seed(companyId)`: POST /companies/:companyId/skill-profiles/seed

    3. Extend `ui/src/api/issues.ts`:
       - `listSubtasks(companyId, issueId)`: GET /companies/:companyId/issues/:issueId/subtasks
       - `createSubtask(companyId, issueId, data)`: POST /companies/:companyId/issues/:issueId/subtasks
       - `addDependency(companyId, issueId, dependsOnId)`: POST /companies/:companyId/issues/:issueId/dependencies
       - `removeDependency(companyId, issueId, dependsOnId)`: DELETE /companies/:companyId/issues/:issueId/dependencies/:dependsOnId

    4. Export new API modules from `ui/src/api/index.ts`

    5. Add query keys to `ui/src/lib/queryKeys.ts`:
       - `webhooks: { list: (companyId) => [...], deliveries: (companyId, webhookId) => [...] }`
       - `skillProfiles: { list: (companyId) => [...] }`
       - `subtasks: { list: (companyId, issueId) => [...] }`

    6. Create `ui/src/components/WebhookEndpointList.tsx`:
       - Receives companyId prop
       - Lists webhook endpoints using TanStack Query (useQuery with queryKeys.webhooks.list)
       - Each endpoint shows: URL (truncated), description, enabled badge (green/red), subscribed event types as chips, consecutiveFailures count if > 0
       - "Add Webhook" button opens a form dialog:
         - URL input (required)
         - Description input (optional)
         - Event type checkboxes (all 6 types, multi-select)
         - Submit calls webhooksApi.create with useMutation
       - Each endpoint has action buttons:
         - Edit (opens same form with pre-filled values)
         - Test (sends test delivery via webhooksApi.testDelivery, shows toast on success)
         - Delete (confirmation dialog, then webhooksApi.remove)
         - "View Deliveries" (expands to show WebhookDeliveryLog)
       - Disabled endpoints show prominent yellow warning banner with "Re-enable" button
       - Use existing UI primitives: Card, Badge, Button, Dialog, Input from ui/src/components/ui/

    7. Create `ui/src/components/WebhookDeliveryLog.tsx`:
       - Receives companyId and webhookId props
       - Lists recent deliveries using TanStack Query (useQuery with queryKeys.webhooks.deliveries)
       - Table columns: Event Type, Status (pending/succeeded/failed/disabled badge), Attempt Count, Last Response Status, Created At
       - Status badges: succeeded = green, failed = red, pending = yellow, disabled = gray
       - Expandable row shows: payload preview (JSON, truncated), lastResponseBody

    8. Extend `ui/src/pages/CompanySettings.tsx`:
       - Add a "Webhooks" section (use existing section/card pattern from the page)
       - Render `<WebhookEndpointList companyId={companyId} />` in the section
       - Position after existing settings sections
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && pnpm vitest run --reporter=verbose</automated>
  </verify>
  <done>
    Webhook API client covers all endpoints, WebhookEndpointList shows CRUD with event type chips and enable/disable, WebhookDeliveryLog shows delivery history with status badges, CompanySettings page has Webhooks section, all existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Subtask tree view and skill profile selector</name>
  <files>
    ui/src/components/SubtaskTree.tsx,
    ui/src/components/SkillProfileSelector.tsx,
    ui/src/pages/IssueDetail.tsx,
    ui/src/pages/AgentDetail.tsx
  </files>
  <action>
    1. Create `ui/src/components/SubtaskTree.tsx`:
       - Receives companyId and parentIssueId props
       - Fetches subtasks via TanStack Query (useQuery with queryKeys.subtasks.list)
       - Renders as an expandable tree:
         - Each subtask row shows: title (link to issue detail), status badge, assignee, priority
         - Dependency arrows: for each subtask that has dependsOn entries, show a visual indicator (e.g., indentation with "depends on: [issue titles]" text, or a simple arrow/line visualization)
         - Use indentation + dependency text labels (not full SVG arrows -- keep it pragmatic)
       - "Add Subtask" button at bottom:
         - Inline form or dialog with: title (required), description (optional), priority dropdown
         - Submit calls issuesApi.createSubtask with useMutation, invalidates subtask list
       - "Add Dependency" action per subtask:
         - Small link/button that opens a dropdown of other subtasks (same parent)
         - Select one to add as dependency
         - Calls issuesApi.addDependency, invalidates list
         - Show toast error if cycle detected (API returns error)
       - Remove dependency: small "x" next to each dependency label
       - Subtask status changes update automatically via query invalidation

    2. Create `ui/src/components/SkillProfileSelector.tsx`:
       - Receives companyId, currentProfileId (string | null), and onChange callback props
       - Fetches profiles via TanStack Query (useQuery with queryKeys.skillProfiles.list)
       - If profiles list is empty, call seed endpoint first (skillProfilesApi.seed) then refetch
       - Renders as a Select dropdown (use existing Select from ui/src/components/ui/select):
         - "No profile" option (clears selection)
         - Divider between builtin and custom profiles
         - Each option shows: profile name, with "(Built-in)" tag for isBuiltin profiles
         - Description shown as helper text below the dropdown
       - onChange fires with the selected profileId (or null for "No profile")

    3. Extend `ui/src/pages/IssueDetail.tsx`:
       - Check if the issue has subtasks (issue.parentId is null AND there are child issues, or just always show the section and let it be empty)
       - Add a "Subtasks" section below existing issue content:
         - Render `<SubtaskTree companyId={companyId} parentIssueId={issue.id} />`
         - Only show for issues that are not subtasks themselves (issue.parentId is null)
       - For subtask issues (parentId is not null), show a "Parent Issue" link at the top pointing to the parent

    4. Extend `ui/src/pages/AgentDetail.tsx`:
       - In the agent settings/configuration area, add a "Skill Profile" section
       - Render `<SkillProfileSelector companyId={companyId} currentProfileId={agent.runtimeConfig?.skillProfileId} onChange={handleProfileChange} />`
       - `handleProfileChange(profileId)`: calls agent update API with `{ runtimeConfig: { ...agent.runtimeConfig, skillProfileId: profileId } }`, invalidates agent query
       - Show current profile description below the selector when a profile is active
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && pnpm vitest run --reporter=verbose</automated>
  </verify>
  <done>
    SubtaskTree shows expandable subtask list with dependency labels and CRUD, SkillProfileSelector dropdown with builtin/custom profiles and auto-seed, IssueDetail has Subtasks section for parent issues, AgentDetail has Skill Profile selector in settings, all existing tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 4 UI features</name>
  <files>ui/src/pages/CompanySettings.tsx, ui/src/pages/IssueDetail.tsx, ui/src/pages/AgentDetail.tsx</files>
  <action>
    Human verifies that all UI components render correctly and function as expected. No automated action needed -- this is a visual checkpoint after Tasks 1 and 2 complete.
  </action>
  <verify>
    Human confirms the following checks pass:
    1. Webhook management in Company Settings -- add, test, view deliveries
    2. Subtask tree on issue detail -- add subtask, add dependency, cycle rejection
    3. Skill profile selector on agent detail -- dropdown, selection, persistence
  </verify>
  <done>All UI features visually confirmed working by operator</done>
</task>

</tasks>

<verification>
- `pnpm vitest run --reporter=verbose` -- all tests pass
- Visual verification of webhook management, subtask tree, and skill profile selector
- All UI components use existing design system (shadcn/radix primitives, Tailwind, TanStack Query)
</verification>

<success_criteria>
- Webhook endpoints visible and manageable in Company Settings
- Delivery history table shows status, attempts, response codes
- Subtask tree renders with dependency labels and supports add/remove
- Skill profile dropdown shows builtin + custom profiles
- All components use existing UI patterns (Card, Badge, Button, Dialog, Select)
</success_criteria>

<output>
After completion, create `.planning/phases/04-notifications-agent-capabilities/04-05-SUMMARY.md`
</output>
