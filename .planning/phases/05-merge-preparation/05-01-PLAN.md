---
phase: 05-merge-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/CONFLICT-MAP.md
autonomous: true
requirements:
  - PREP-01
  - PREP-02

must_haves:
  truths:
    - "Every conflicting file from the dry-run merge is documented with area category, fork changes, upstream changes, and resolution strategy"
    - "The 2-3 hardest conflicts have line-by-line deep-dive analysis identifying exactly what to keep from each side"
    - "A pre-merge git tag and backup branch exist as rollback safety net"
    - "Conflict map is actionable enough that phases 6-7 can follow it as a merge playbook"
  artifacts:
    - path: ".planning/CONFLICT-MAP.md"
      provides: "Per-file conflict analysis for all 16 conflicting files with resolution strategies"
      contains: "server/src/index.ts"
  key_links:
    - from: ".planning/CONFLICT-MAP.md"
      to: "phases 6-7 merge execution"
      via: "resolution strategies referenced during actual merge"
      pattern: "Resolution Strategy"
---

<objective>
Map all 16 upstream merge conflicts with categorized resolution strategies and establish rollback safety net.

Purpose: De-risk the actual merge in phases 6-7 by documenting every conflict with a concrete resolution plan, including deep-dive analysis on the hardest conflicts. Create the safety net (tag + branch) so merge work can be safely reverted.
Output: `.planning/CONFLICT-MAP.md` with per-file strategies, git tag `pre-upstream-sync`, git branch `pre-upstream-sync-backup`
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-merge-preparation/05-RESEARCH.md
@.planning/phases/05-merge-preparation/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conflict map with per-file resolution strategies</name>
  <files>.planning/CONFLICT-MAP.md</files>
  <action>
Create `.planning/CONFLICT-MAP.md` documenting all 16 conflicting files from the upstream merge dry-run. Structure the document as follows:

**Header section:** Summary stats (16 conflicts, 125 ahead/228 behind, merge command reference).

**Group files by area:**

1. **DB Migrations (3 files):** `_journal.json`, `0026_snapshot.json`, `0027_snapshot.json`
   - Resolution: "Fully resolved by migration renumbering in PREP-03 -- fork keeps 0026-0030, upstream becomes 0031-0032"

2. **Server (4 files):** `app.ts`, `index.ts`, `heartbeat.ts`, `issues.ts`
   - For `server/src/index.ts` (HARD): Deep-dive analysis. Fork state: 973 lines, exports `startServer()` with all fork route registrations (github, webhooks, skill-profiles), webhook dispatcher startup, analytics middleware. Upstream state: 693 lines, restructured with workspace runtime service reconciliation, `reconcilePersistedRuntimeServicesOnStartup` import, onboarding wizard, instance settings routes. Resolution: Manual merge -- keep all fork additions while accepting upstream's new imports and startup code. Document the specific sections that need reconciliation.
   - For `server/src/services/heartbeat.ts` (HARD): Deep-dive analysis. Fork state: 2892 lines with token usage tracking, budget enforcement, skill profile integration, context optimization pipeline, webhook event dispatch. Upstream state: 2550 lines with workspace runtime service management, execution workspace policy, heartbeat run summary, issue goal fallback. Resolution: Manual merge -- both sets of modifications target different concerns and should coexist. Identify exact insertion points.
   - For `server/src/app.ts` (EASY): ~10 insertions, 11 deletions. Accept both -- likely import additions and middleware from both sides.
   - For `server/src/services/issues.ts` (MEDIUM): Fork added webhook dispatching on issue events, upstream added workspace-related issue fields. Accept both.

3. **UI (5 files):** `App.tsx`, `agent-config-primitives.tsx`, `queryKeys.ts`, `AgentDetail.tsx`, `Costs.tsx`
   - For `App.tsx` (MEDIUM): Fork has `GitHubSetupComplete` route, upstream adds `InstanceSettings`, `NotFound`, `RunTranscriptUxLab` routes. Keep both sets of routes.
   - For `AgentDetail.tsx` (MEDIUM-HARD): Fork added analytics tabs (token breakdown, trace view, context utilization), upstream restructured agent detail layout. Needs careful merge.
   - For `Costs.tsx` (MEDIUM): Fork added AnalyticsCharts, upstream modified cost display.
   - For `agent-config-primitives.tsx` (MEDIUM): Both sides modified agent config form fields.
   - For `queryKeys.ts` (EASY): Both added new query keys. Combine both.

4. **Shared Packages (1 file):** `packages/adapter-utils/src/index.ts`
   - EASY: Both sides added exports. Combine both.

5. **Infrastructure (2 files):** `Dockerfile`, `ui/package.json`
   - `Dockerfile` (MEDIUM): Both modified Docker build. Compare specific changes.
   - `ui/package.json` (EASY): Both added dependencies. Combine and regenerate lockfile.

6. **Lockfile (1 file):** `pnpm-lock.yaml`
   - Always regenerate from scratch after all package.json conflicts resolved. Resolution: Delete and run `pnpm install`.

**For each file, document:** Area category, difficulty (EASY/MEDIUM/HARD), what fork changed, what upstream changed, resolution strategy (keep ours/keep theirs/manual merge/regenerate), and any notes for the merge executor.

**For the 2 HARD conflicts (`server/src/index.ts` and `server/src/services/heartbeat.ts`):**
Run the actual dry-run merge to inspect conflict markers:
```bash
git merge --no-commit --no-ff upstream/master 2>/dev/null
```
Then for each hard conflict file, examine the conflict markers to identify:
- Exact line ranges where conflicts occur
- Which fork code blocks must be preserved
- Which upstream code blocks to accept
- Recommended merge order (which side's changes to apply first)

After extracting the analysis, abort the merge:
```bash
git merge --abort
```

**Summary table** at the end: File | Area | Difficulty | Strategy
  </action>
  <verify>
    <automated>test -f .planning/CONFLICT-MAP.md && grep -c "Resolution Strategy\|resolution strategy\|Resolution:" .planning/CONFLICT-MAP.md | awk '{if ($1 >= 10) print "PASS: at least 10 resolution strategies documented"; else print "FAIL: fewer than 10 resolution strategies"}'</automated>
  </verify>
  <done>CONFLICT-MAP.md exists with all 16 conflicting files documented, grouped by area (DB, server, UI, shared, infra, lockfile), each with resolution strategy. The 2 hardest conflicts (server/src/index.ts, server/src/services/heartbeat.ts) have deep-dive line-by-line analysis with specific code blocks to keep/accept.</done>
</task>

<task type="auto">
  <name>Task 2: Create rollback safety net (git tag and backup branch)</name>
  <files></files>
  <action>
Create the pre-merge rollback safety net per the locked decisions:

1. Create a git tag at the current HEAD:
   ```bash
   git tag pre-upstream-sync
   ```

2. Create a backup branch at the current HEAD:
   ```bash
   git branch pre-upstream-sync-backup
   ```

3. Verify both exist:
   ```bash
   git tag -l pre-upstream-sync
   git branch -l pre-upstream-sync-backup
   ```

These provide two rollback options during phases 6-7:
- `git revert -m 1 <merge-commit>` for per-chunk rollback (requires merge commits, not squash)
- `git reset --hard pre-upstream-sync` for full rollback to pre-merge state

Note: Per-chunk tags (e.g., `post-chunk-1-db`) will be created during actual merge execution in phases 6-7, not now.
  </action>
  <verify>
    <automated>git tag -l pre-upstream-sync | grep -q "pre-upstream-sync" && git branch -l pre-upstream-sync-backup | grep -q "pre-upstream-sync-backup" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Git tag `pre-upstream-sync` and branch `pre-upstream-sync-backup` both exist at the current HEAD, providing a rollback safety net for merge phases 6-7.</done>
</task>

</tasks>

<verification>
- `.planning/CONFLICT-MAP.md` exists and documents all 16 conflicting files
- Each file has area category, difficulty rating, and resolution strategy
- Deep-dive sections exist for `server/src/index.ts` and `server/src/services/heartbeat.ts`
- `git tag -l pre-upstream-sync` returns the tag
- `git branch -l pre-upstream-sync-backup` returns the branch
</verification>

<success_criteria>
- All 16 conflicting files are documented with per-file resolution strategies
- Conflict map is grouped by area (DB, server, UI, shared packages, infrastructure, lockfile)
- 2 hardest conflicts have line-by-line deep-dive analysis
- Pre-merge git tag and backup branch exist as rollback safety net
- Conflict map is actionable enough to serve as a merge playbook for phases 6-7
</success_criteria>

<output>
After completion, create `.planning/phases/05-merge-preparation/05-01-SUMMARY.md`
</output>
