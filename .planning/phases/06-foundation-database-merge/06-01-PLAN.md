---
phase: 06-foundation-database-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/migrations/meta/_journal.json
  - packages/db/src/migrations/meta/0026_snapshot.json
  - packages/db/src/migrations/meta/0027_snapshot.json
  - packages/adapter-utils/src/index.ts
  - server/src/index.ts
  - server/src/services/heartbeat.ts
  - server/src/app.ts
  - server/src/services/issues.ts
  - ui/src/App.tsx
  - ui/src/pages/AgentDetail.tsx
  - ui/src/pages/Costs.tsx
  - ui/src/components/agent-config-primitives.tsx
  - ui/src/lib/queryKeys.ts
  - Dockerfile
  - ui/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "All 16 upstream merge conflicts are resolved in a single merge commit with two parents (--no-ff)"
    - "packages/shared, packages/adapter-utils, and packages/db compile with zero TypeScript errors"
    - "The combined migration set (fork 0026-0030 + renumbered upstream 0031-0032) has a continuous snapshot chain"
    - "All 455+ tests pass including v1.0 canary exports (32 assertions) and API smoke tests (4 route groups)"
    - "pnpm build succeeds across all packages"
    - "A post-foundation-merge git tag exists for phase 7 rollback safety"
  artifacts:
    - path: "packages/db/src/migrations/meta/_journal.json"
      provides: "Fork migration journal preserved (idx 0-32)"
      contains: "lying_pete_wisdom"
    - path: "packages/adapter-utils/src/index.ts"
      provides: "Combined fork + upstream exports"
      contains: "buildBrowserConfig"
    - path: "server/src/index.ts"
      provides: "Server entry with fork + upstream features"
      contains: "reconcilePersistedRuntimeServicesOnStartup"
    - path: "server/src/services/heartbeat.ts"
      provides: "Heartbeat with fork token tracking + upstream workspace runtime"
      contains: "createUsageTracker"
  key_links:
    - from: "server/src/services/heartbeat.ts"
      to: "packages/db"
      via: "drizzle schema imports"
      pattern: "from.*@paperclipai/db"
    - from: "server/src/index.ts"
      to: "server/src/app.ts"
      via: "createApp import"
      pattern: "createApp"
    - from: "pnpm-lock.yaml"
      to: "ui/package.json"
      via: "lockfile regeneration"
      pattern: "pnpm-lock.yaml"
---

<objective>
Execute the upstream merge and resolve all 16 conflicts to produce a single verified merge commit.

Purpose: This is the core merge operation that brings 226 upstream commits into the fork. All conflicts must be resolved in one merge commit (git requirement). The conflict map from phase 5 provides exact resolution strategies for every file.

Output: A merge commit with all upstream changes integrated, a clean CI run (typecheck + 455+ tests + build), and a `post-foundation-merge` tag for phase 7 rollback safety.
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONFLICT-MAP.md
@.planning/FEATURE-MANIFEST.md
@.planning/phases/06-foundation-database-merge/06-RESEARCH.md
@.planning/phases/05-merge-preparation/05-01-SUMMARY.md
@.planning/phases/05-merge-preparation/05-02-SUMMARY.md
@.planning/phases/05-merge-preparation/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute merge and resolve all 16 conflicts</name>
  <files>
    packages/db/src/migrations/meta/_journal.json,
    packages/db/src/migrations/meta/0026_snapshot.json,
    packages/db/src/migrations/meta/0027_snapshot.json,
    packages/adapter-utils/src/index.ts,
    server/src/index.ts,
    server/src/services/heartbeat.ts,
    server/src/app.ts,
    server/src/services/issues.ts,
    ui/src/App.tsx,
    ui/src/pages/AgentDetail.tsx,
    ui/src/pages/Costs.tsx,
    ui/src/components/agent-config-primitives.tsx,
    ui/src/lib/queryKeys.ts,
    Dockerfile,
    ui/package.json,
    pnpm-lock.yaml
  </files>
  <action>
    IMPORTANT: Read `.planning/CONFLICT-MAP.md` before starting -- it has per-file resolution strategies and deep-dive analysis for the 2 HARD conflicts.

    **Step 1: Safety check.** Verify `pre-upstream-sync` tag exists: `git tag -l pre-upstream-sync`. If missing, STOP and abort.

    **Step 2: Start the merge.** Run `git merge --no-ff --no-commit upstream/master`. This will report 16 conflicts. Verify the count matches.

    **Step 3: Resolve conflicts in dependency order** (packages that others depend on first). Follow the CONFLICT-MAP.md strategies exactly:

    **Group A -- DB Migrations (3 files, EASY):**
    ```bash
    git checkout --ours packages/db/src/migrations/meta/_journal.json
    git checkout --ours packages/db/src/migrations/meta/0026_snapshot.json
    git checkout --ours packages/db/src/migrations/meta/0027_snapshot.json
    git add packages/db/src/migrations/meta/_journal.json \
          packages/db/src/migrations/meta/0026_snapshot.json \
          packages/db/src/migrations/meta/0027_snapshot.json
    ```
    Reason: Phase 5 already renumbered upstream's 0026-0027 to 0031-0032. Fork's versions must be preserved.

    **Group B -- Shared Packages (1 file, EASY):**
    - `packages/adapter-utils/src/index.ts`: Open the file, find conflict markers. Both sides added different exports. Combine ALL exports from both sides -- fork's context pipeline/token estimation exports AND upstream's workspace runtime type exports. Remove conflict markers. `git add`.

    **Group C -- Server (4 files, 2 EASY + 2 HARD):**
    - `server/src/app.ts` (EASY): Both sides added imports. Keep fork's `instanceHeartbeatSettingsRoutes` import and add upstream's `instanceSettingsRoutes` and `onboardingWizardRoutes` imports. Same for route registrations. `git add`.
    - `server/src/services/issues.ts` (MEDIUM): Both sides added different features (fork: dependency graph + webhook dispatch; upstream: workspace policy + goal fallback). Accept both sets of additions. Fork's changes are much larger (~275 lines vs 1 line from upstream). `git add`.
    - `server/src/services/heartbeat.ts` (HARD -- 6 conflict zones): Follow CONFLICT-MAP.md deep-dive analysis precisely:
      - Zone 1 (DB imports): Keep both -- fork's `issueComments, issueLabels, labels` + upstream's `projects, projectWorkspaces`.
      - Zone 2 (Utility imports): Keep both -- fork's `asString` + upstream's `costService`.
      - Zone 3 (Service imports): Keep both sets entirely -- fork's token/budget/skills imports + upstream's workspace/runtime imports.
      - Zone 4 (Service init): Keep both -- fork's `githubSvc` + upstream's `issuesSvc`.
      - Zone 5 (Pre-adapter): Keep fork's GitHub token injection + Lightpanda browser env injection, THEN add upstream's runtime service lifecycle block after.
      - Zone 6 (Post-adapter): Keep fork's adapter execution wrapper (try/finally + usage flush), THEN add upstream's runtime service persistence after usage flush.
      Start with fork's version as base and layer upstream additions. The canary tests will verify nothing was dropped.
    - `server/src/index.ts` (HARD -- 3 conflict zones): Follow CONFLICT-MAP.md deep-dive analysis precisely:
      - Zone 1: Remove fork's duplicate embedded PostgreSQL block (it already appears earlier). Keep deployment mode validation from both sides.
      - Zone 2: Keep fork's full server setup sequence (BetterAuth, createApp, createServer, WebSocket, heartbeat scheduler, webhook dispatcher, backup scheduler, server.listen). Add upstream's `reconcilePersistedRuntimeServicesOnStartup` import at top and call after WebSocket setup.
      - Zone 3: Keep fork's bootstrap invite + shutdown handlers. Upstream's env vars and WebSocket should already be present from Zone 2.
      Start with fork's version as base and add upstream's new imports + `reconcilePersistedRuntimeServicesOnStartup` call.

    **Group D -- UI (5 files, EASY to MEDIUM-HARD):**
    - `ui/src/lib/queryKeys.ts` (EASY): Combine both key sets -- fork's `skillProfiles, webhookEndpoints, webhookDeliveries, tokenAnalytics, contextUtilization` + upstream's `instanceSettings, workspaceRuntimeServices`. `git add`.
    - `ui/src/components/agent-config-primitives.tsx` (MEDIUM): Both added different form fields. Keep fork's skill profile/budget config fields AND upstream's execution workspace/runtime fields. `git add`.
    - `ui/src/App.tsx` (MEDIUM): Keep fork's `GitHubSetupComplete` route + add upstream's `InstanceSettings`, `NotFound`, `RunTranscriptUxLab` routes. Both import and route definition additions. `git add`.
    - `ui/src/pages/Costs.tsx` (MEDIUM): Keep fork's "By Agent"/"By Project" breakdown cards + `AnalyticsCharts`. Add upstream's budget utilization bar. Both serve complementary purposes. `git add`.
    - `ui/src/pages/AgentDetail.tsx` (MEDIUM-HARD): Conflict zone 1 (imports) -- combine both. Conflict zone 2 (tab structure) -- integrate fork's analytics tabs (token breakdown, trace view, context utilization) into upstream's new tab organization. Fork tabs need to fit upstream's restructured layout. `git add`.

    **Group E -- Infrastructure (2 files, EASY/MEDIUM):**
    - `Dockerfile` (MEDIUM): Keep ours (fork is superset -- adds Lightpanda, GitHub CLI, gosu privilege drop, custom entrypoint). Run `git checkout --ours Dockerfile && git add Dockerfile`.
    - `ui/package.json` (EASY): Combine both dependency additions (fork's `recharts`, `@standardwebhooks/standard-webhooks` + upstream's dependencies). `git add`.

    **Group F -- Lockfile (LAST, SPECIAL):**
    - Delete `pnpm-lock.yaml` entirely. Run `pnpm install` to regenerate from all resolved package.json files. `git add pnpm-lock.yaml`.

    **Step 4: Verify no unresolved conflicts remain.** Run `git diff --check` -- should report nothing. Run `git status` -- no "both modified" files should remain.

    **Rollback if broken:** If conflict resolution produces code that obviously won't work, run `git merge --abort` to return to pre-merge state and try again. Per user decision: rollback over fix-forward for merge errors.
  </action>
  <verify>
    <automated>git diff --check && git status | grep -c "both modified" | grep -q "^0$" && echo "All conflicts resolved"</automated>
  </verify>
  <done>All 16 conflicts resolved, all files staged, no unresolved conflict markers remain, git status shows merge in progress with everything staged</done>
</task>

<task type="auto">
  <name>Task 2: Verify CI suite, commit merge, and tag</name>
  <files>pnpm-lock.yaml</files>
  <action>
    **Step 1: Run foundation-first typecheck** to catch issues early:
    ```bash
    pnpm --filter @paperclipai/shared typecheck
    pnpm --filter @paperclipai/adapter-utils typecheck
    pnpm --filter @paperclipai/db typecheck
    ```
    If type errors appear in auto-merged files (e.g., upstream schema additions referencing unknown types), add minimal type stubs or fix imports. Per user decision: fix forward on test failures.

    **Step 2: Run full typecheck across all packages:**
    ```bash
    pnpm -r typecheck
    ```
    If server or UI typecheck fails due to upstream additions that reference new types/services, fix forward -- add missing imports, type stubs, or adjust references until green. This is expected behavior per research (upstream added new modules that may need wiring).

    **Step 3: Run the full test suite (455+ tests):**
    ```bash
    pnpm test:run
    ```
    This includes the v1.0 canary export tests (32 assertions in `server/src/__tests__/v1-feature-exports.test.ts`) and API smoke tests (4 route groups in `server/src/__tests__/v1-api-smoke.test.ts`). If any canary test fails, a fork export was dropped during merge -- go back and fix the conflict resolution. Per user decision: fix forward on test failures.

    **Step 4: Build all packages:**
    ```bash
    pnpm -r build
    ```

    **Step 5: (Optional) Run migration compat script if DATABASE_URL is set:**
    ```bash
    if [ -n "$DATABASE_URL" ]; then npx tsx scripts/check-migration-compat.ts; fi
    ```

    **Step 6: Commit the merge.** This creates the merge commit with two parents (fork + upstream):
    ```bash
    git commit -m "merge: integrate 226 upstream commits into fork

    Resolves all 16 file conflicts following the conflict map playbook.
    Foundation packages (shared, adapter-utils, db) verified via typecheck.
    Full CI suite passing (typecheck + tests + build).

    Conflict resolution summary:
    - DB migrations: kept fork versions (upstream renumbered to 0031-0032 in phase 5)
    - Shared packages: combined fork + upstream exports
    - Server: merged fork features (tokens, webhooks, skills) with upstream (workspace runtime, onboarding)
    - UI: merged fork analytics with upstream settings/onboarding routes
    - Infrastructure: kept fork Dockerfile (superset), combined deps
    - Lockfile: regenerated from scratch"
    ```

    **Step 7: Create the post-merge tag** for phase 7 rollback safety:
    ```bash
    git tag post-foundation-merge
    ```

    **Rollback if verification fails catastrophically:** If typecheck/tests/build fail in ways that can't be fixed forward, roll back the entire merge:
    ```bash
    git merge --abort  # if not yet committed
    # OR
    git reset --hard pre-upstream-sync  # if committed but broken
    ```
    Then analyze what went wrong and try again. Per user decision: rollback over fix-forward for merge errors, fix-forward for test failures.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm test:run && pnpm -r build && git log --oneline -1 --merges | grep -q "merge" && git tag -l post-foundation-merge | grep -q "post-foundation-merge" && echo "PASS"</automated>
  </verify>
  <done>Full CI suite passes (typecheck + 455+ tests + build), merge commit exists with two parents, post-foundation-merge tag created</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the phase goal:

1. **Merge commit structure:** `git log --oneline -1 --merges` shows merge commit. `git cat-file -p HEAD` shows two parent commits.
2. **Foundation packages compile:** `pnpm --filter @paperclipai/shared typecheck && pnpm --filter @paperclipai/adapter-utils typecheck && pnpm --filter @paperclipai/db typecheck` all succeed.
3. **Full CI green:** `pnpm -r typecheck && pnpm test:run && pnpm -r build` all succeed.
4. **Canary tests pass:** `pnpm test:run -- --grep "v1.0 fork feature"` shows 32 passing assertions.
5. **Smoke tests pass:** `pnpm test:run -- --grep "v1.0 API smoke"` shows all route groups passing.
6. **Tag exists:** `git tag -l post-foundation-merge` returns the tag.
7. **No conflict markers remain:** `grep -r "<<<<<<< HEAD" packages/ server/ ui/ Dockerfile` returns nothing.
</verification>

<success_criteria>
- All 16 upstream conflicts resolved in a single merge commit with `--no-ff` (two parents)
- `packages/shared`, `packages/adapter-utils`, and `packages/db` compile with zero TypeScript errors
- Combined migration set (fork 0026-0030 + upstream 0031-0032) has continuous snapshot chain
- Full test suite passes (455+ tests including 32 canary export assertions + 4 API smoke tests)
- `pnpm -r build` succeeds across all packages
- `post-foundation-merge` git tag exists for phase 7 rollback safety
- No conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`) remain in any source file
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-database-merge/06-01-SUMMARY.md`
</output>
