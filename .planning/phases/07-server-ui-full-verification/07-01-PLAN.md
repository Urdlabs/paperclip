---
phase: 07-server-ui-full-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/__tests__/workspace-runtime.test.ts
  - cli/src/__tests__/worktree.test.ts
  - server/src/__tests__/v1-integration-token-analytics.test.ts
  - server/src/__tests__/v1-integration-context-optimization.test.ts
  - server/src/__tests__/v1-integration-webhooks.test.ts
  - server/src/__tests__/v1-integration-traces.test.ts
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-04

must_haves:
  truths:
    - "All 545+ tests pass with zero failures (3 timeout tests fixed)"
    - "Token analytics routes return expected response shapes"
    - "Context optimization pipeline produces expected output"
    - "Webhook CRUD endpoints respond with correct status codes and shapes"
    - "Trace and activity endpoints return correct response structures"
  artifacts:
    - path: "server/src/__tests__/workspace-runtime.test.ts"
      provides: "Fixed timeout on 2 workspace runtime tests"
      contains: "15_000"
    - path: "cli/src/__tests__/worktree.test.ts"
      provides: "Fixed timeout on 1 worktree test"
      contains: "15_000"
    - path: "server/src/__tests__/v1-integration-token-analytics.test.ts"
      provides: "Integration tests for token analytics API contracts"
      exports: []
    - path: "server/src/__tests__/v1-integration-context-optimization.test.ts"
      provides: "Integration tests for context optimization pipeline"
      exports: []
    - path: "server/src/__tests__/v1-integration-webhooks.test.ts"
      provides: "Integration tests for webhook CRUD API contracts"
      exports: []
    - path: "server/src/__tests__/v1-integration-traces.test.ts"
      provides: "Integration tests for trace and run detail API contracts"
      exports: []
  key_links:
    - from: "server/src/__tests__/v1-integration-token-analytics.test.ts"
      to: "server/src/routes/costs.ts"
      via: "supertest route mounting"
      pattern: "costRoutes"
    - from: "server/src/__tests__/v1-integration-webhooks.test.ts"
      to: "server/src/routes/webhooks.ts"
      via: "supertest route mounting"
      pattern: "webhookRoutes"
    - from: "server/src/__tests__/v1-integration-traces.test.ts"
      to: "server/src/routes/activity.ts"
      via: "supertest route mounting"
      pattern: "activityRoutes"
---

<objective>
Fix the 3 failing upstream timeout tests and add integration tests for v1.0 features 1-4 (token analytics, context optimization, webhooks, traces).

Purpose: Eliminate all test failures to satisfy VERIFY-01, and begin VERIFY-04 coverage for the first four v1.0 feature areas with permanent regression tests.
Output: 2 modified test files (timeout fixes) + 4 new integration test files.
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-server-ui-full-verification/07-RESEARCH.md

@server/src/__tests__/v1-api-smoke.test.ts
@server/src/__tests__/workspace-runtime.test.ts
@cli/src/__tests__/worktree.test.ts

<interfaces>
<!-- Existing patterns the executor must follow -->

From server/src/__tests__/v1-api-smoke.test.ts:
Pattern for all integration tests:
```typescript
// 1. Create mock services with vi.hoisted()
const mockService = vi.hoisted(() => ({
  methodName: vi.fn().mockResolvedValue(/* mock data */),
}));

// 2. Mock the service module
vi.mock("../services/index.js", () => ({
  serviceName: () => mockService,
  logActivity: vi.fn(),
}));

// 3. Create express app with actor middleware
function createApp() {
  const app = express();
  app.use(express.json());
  app.use((req, _res, next) => {
    (req as any).actor = {
      type: "board", userId: "user-1",
      companyIds: ["company-1"], source: "session",
      isInstanceAdmin: true,
    };
    next();
  });
  app.use("/api", routesFn({} as any));
  app.use(errorHandler);
  return app;
}

// 4. Test HTTP contracts
it("GET /api/... returns expected shape", async () => {
  const res = await request(createApp()).get("/api/...");
  expect(res.status).toBe(200);
  expect(res.body).toHaveProperty("expectedField");
});
```

Route files to test:
- server/src/routes/costs.ts - costRoutes (token analytics)
- server/src/routes/webhooks.ts - webhookRoutes (webhooks)
- server/src/routes/activity.ts - activityRoutes (traces + activity)
- server/src/context-pipeline/ - runContextPipeline (context optimization, service-level test)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 upstream timeout test failures</name>
  <files>server/src/__tests__/workspace-runtime.test.ts, cli/src/__tests__/worktree.test.ts</files>
  <action>
Fix the 3 failing tests by increasing their timeout from the default 5000ms to 15000ms. The tests pass in isolation (2500-3200ms) but exceed the 5s default under concurrent load.

In `server/src/__tests__/workspace-runtime.test.ts`, find the two failing `it()` calls:
1. "creates and reuses a git worktree for an issue-scoped branch" -- add `, 15_000` as the third argument to `it()`
2. "runs a configured provision command inside the derived worktree" -- add `, 15_000` as the third argument to `it()`

In `cli/src/__tests__/worktree.test.ts`, find the one failing `it()` call:
1. "copies shared git hooks into a linked worktree git dir" -- add `, 15_000` as the third argument to `it()`

Do NOT change any test logic or assertions. Only add the timeout parameter.

There is already precedent for this: `worktree.test.ts` line ~404 uses `20_000` for the "worktree:make" test.

Verify all 3 files still parse correctly after the change.
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && npx vitest run server/src/__tests__/workspace-runtime.test.ts cli/src/__tests__/worktree.test.ts --reporter=verbose</automated>
  </verify>
  <done>Both test files pass with zero failures. The 3 previously-failing tests now complete within their 15s timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for v1.0 features 1-4</name>
  <files>server/src/__tests__/v1-integration-token-analytics.test.ts, server/src/__tests__/v1-integration-context-optimization.test.ts, server/src/__tests__/v1-integration-webhooks.test.ts, server/src/__tests__/v1-integration-traces.test.ts</files>
  <action>
Create 4 new integration test files following the exact supertest + vi.mock pattern from `v1-api-smoke.test.ts`. Each file tests one v1.0 feature area. These are PERMANENT regression tests.

**File 1: v1-integration-token-analytics.test.ts**
Tests the costs routes (token analytics). Mount `costRoutes` on express. Mock `costService` (from `../services/index.js`) with: `summary` (returns object with totalCostCents, budgetMonthlyCents, totalTokens, cacheHitRate, avgTokensPerRun, avgCompressionRatio), `byAgent` (returns array), `timeSeries` (returns array), `contextComposition` (returns object), `byProject` (returns array). Also mock `companyService` (update) and `agentService` (getById, update). Mock `logActivity` from both `../services/index.js` and `../services/activity-log.js`.

Test at minimum:
- GET /api/companies/company-1/costs/summary returns 200 with totalTokens, cacheHitRate, avgTokensPerRun, avgCompressionRatio
- GET /api/companies/company-1/costs/by-agent returns 200 with array
- GET /api/companies/company-1/costs/time-series returns 200 with array
- GET /api/companies/company-1/costs/context-composition returns 200 with object
- GET /api/companies/company-1/costs/by-project returns 200 with array

**File 2: v1-integration-context-optimization.test.ts**
Tests the context pipeline (service-level, no routes). Import and test `runContextPipeline` from `../context-pipeline/index.js`. This is a pure function test, not a route test. Provide a mock PipelineContext with issueBody, issueComments, projectContext, relatedIssues, and verify that the pipeline returns an object with optimizedContext (string), taskType (string), and that the output is shorter than or equal to the input (context was compressed/serialized). Also test that essential keys (issueId, projectId) are preserved through the pipeline. No vi.mock needed -- the pipeline is a pure reduce function.

**File 3: v1-integration-webhooks.test.ts**
Tests webhook CRUD routes. Mount `webhookRoutes` on express. Mock `webhookService` (from `../services/webhooks.js`) with: create (returns mock webhook), list (returns array), getById (returns mock), update (returns mock), delete (returns void). Mock `generateWebhookSecret` from same module. Also mock `logActivity`.

Test at minimum:
- GET /api/companies/company-1/webhooks returns 200 with array
- POST /api/companies/company-1/webhooks with valid body returns 201
- GET /api/companies/company-1/webhooks/:id returns 200 with webhook object
- DELETE /api/companies/company-1/webhooks/:id returns 204

Before writing these tests, read `server/src/routes/webhooks.ts` to confirm the exact route paths, HTTP methods, and which service methods each route calls. Adjust mock shape accordingly.

**File 4: v1-integration-traces.test.ts**
Tests trace/activity routes. Mount `activityRoutes` on express. Mock `activityService` (from `../services/index.js`) with: list (returns array), getRunsForIssue (returns array), getIssuesForRun (returns array). Also mock `logActivity`.

Test at minimum:
- GET /api/companies/company-1/activity returns 200 with array
- GET /api/companies/company-1/issues/:issueId/runs returns 200 with array
- GET /api/companies/company-1/heartbeat-runs/:runId/issues returns 200 with array

Before writing these tests, read `server/src/routes/activity.ts` to confirm the exact route paths and service method names. The activity routes use `activityService` from the services index.

**For all route test files:** Always read the actual route file first to confirm the exact route paths, HTTP methods, and service methods invoked. Mock only the service methods that the tested routes actually call. Always include `app.use(errorHandler)` after route mounting. Always mock `logActivity` from both `../services/index.js` and `../services/activity-log.js` to prevent unhandled import errors (see Pitfall 3 in research).
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && npx vitest run server/src/__tests__/v1-integration-token-analytics.test.ts server/src/__tests__/v1-integration-context-optimization.test.ts server/src/__tests__/v1-integration-webhooks.test.ts server/src/__tests__/v1-integration-traces.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 4 integration test files pass. Token analytics endpoints return expected fields (totalTokens, cacheHitRate, etc.). Context pipeline produces optimized output preserving essential keys. Webhook CRUD endpoints return correct status codes. Trace endpoints return correct response structures.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run full test suite: `npx vitest run --reporter=verbose` -- all tests pass (545+ including new integration tests)
2. Verify no regressions: existing 542 tests still pass
3. Verify timeout fixes: workspace-runtime and worktree tests pass in full suite (not just isolation)
</verification>

<success_criteria>
- Zero test failures in full suite (was 3, now 0)
- 4 new integration test files exist and pass
- Total test count is 545+ (542 existing + 3 fixed + new integration assertions)
- All tests follow established supertest + vi.mock pattern
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-ui-full-verification/07-01-SUMMARY.md`
</output>
