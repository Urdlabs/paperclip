---
phase: 07-server-ui-full-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/__tests__/v1-integration-activity-feeds.test.ts
  - server/src/__tests__/v1-integration-task-decomposition.test.ts
  - server/src/__tests__/v1-integration-skill-profiles.test.ts
  - server/src/__tests__/v1-integration-code-review.test.ts
autonomous: true
requirements:
  - VERIFY-04

must_haves:
  truths:
    - "Activity feed endpoints return expected list and create response shapes"
    - "Task decomposition dependency endpoints respond with correct status codes"
    - "Skill profile CRUD endpoints return correct status codes and shapes"
    - "Code review service functions produce expected output structures"
  artifacts:
    - path: "server/src/__tests__/v1-integration-activity-feeds.test.ts"
      provides: "Integration tests for activity feed API contracts"
      exports: []
    - path: "server/src/__tests__/v1-integration-task-decomposition.test.ts"
      provides: "Integration tests for task decomposition dependency API"
      exports: []
    - path: "server/src/__tests__/v1-integration-skill-profiles.test.ts"
      provides: "Integration tests for skill profile CRUD API"
      exports: []
    - path: "server/src/__tests__/v1-integration-code-review.test.ts"
      provides: "Integration tests for code review service contracts"
      exports: []
  key_links:
    - from: "server/src/__tests__/v1-integration-activity-feeds.test.ts"
      to: "server/src/routes/activity.ts"
      via: "supertest route mounting"
      pattern: "activityRoutes"
    - from: "server/src/__tests__/v1-integration-task-decomposition.test.ts"
      to: "server/src/routes/issues.ts"
      via: "supertest route mounting"
      pattern: "issueRoutes"
    - from: "server/src/__tests__/v1-integration-skill-profiles.test.ts"
      to: "server/src/routes/skill-profiles.ts"
      via: "supertest route mounting"
      pattern: "skillProfileRoutes"
    - from: "server/src/__tests__/v1-integration-code-review.test.ts"
      to: "server/src/services/code-review.ts"
      via: "direct service import"
      pattern: "parsePrUrl|buildReviewPayload"
---

<objective>
Add integration tests for v1.0 features 5-8 (activity feeds, task decomposition, skill profiles, code review).

Purpose: Complete VERIFY-04 coverage for all 8 v1.0 feature areas with permanent regression tests. This plan runs in parallel with plan 01 since there are no file overlaps.
Output: 4 new integration test files.
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-server-ui-full-verification/07-RESEARCH.md

@server/src/__tests__/v1-api-smoke.test.ts
@server/src/__tests__/activity-routes.test.ts

<interfaces>
<!-- Existing patterns the executor must follow -->

From server/src/__tests__/v1-api-smoke.test.ts:
Pattern for all integration tests:
```typescript
// 1. Create mock services with vi.hoisted()
const mockService = vi.hoisted(() => ({
  methodName: vi.fn().mockResolvedValue(/* mock data */),
}));

// 2. Mock the service module
vi.mock("../services/index.js", () => ({
  serviceName: () => mockService,
  logActivity: vi.fn(),
}));

// 3. Create express app with actor middleware
function createApp() {
  const app = express();
  app.use(express.json());
  app.use((req, _res, next) => {
    (req as any).actor = {
      type: "board", userId: "user-1",
      companyIds: ["company-1"], source: "session",
      isInstanceAdmin: true,
    };
    next();
  });
  app.use("/api", routesFn({} as any));
  app.use(errorHandler);
  return app;
}
```

Route files to test:
- server/src/routes/activity.ts - activityRoutes (activity feeds)
- server/src/routes/issues.ts - issueRoutes (task decomposition / dependencies)
- server/src/routes/skill-profiles.ts - skillProfileRoutes (skill profiles)
- server/src/services/code-review.ts - parsePrUrl, buildReviewPayload (code review, service-level)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for activity feeds and task decomposition</name>
  <files>server/src/__tests__/v1-integration-activity-feeds.test.ts, server/src/__tests__/v1-integration-task-decomposition.test.ts</files>
  <action>
Create 2 new integration test files following the established supertest + vi.mock pattern.

**File 1: v1-integration-activity-feeds.test.ts**
Tests the activity feed endpoints (distinct from traces tested in plan 01 -- this focuses on the company activity list and activity creation). Mount `activityRoutes` on express. Mock `activityService` (from `../services/index.js`) with: list (returns array of activity objects with id, action, createdAt), create (returns mock activity). Also mock `logActivity`.

Test at minimum:
- GET /api/companies/company-1/activity returns 200 with array
- POST /api/companies/company-1/activity with valid body returns 201 (or 200 depending on route implementation)

Before writing tests, read `server/src/routes/activity.ts` to confirm the exact route paths for company-scoped activity endpoints and the service methods they call. Pay special attention to whether POST creates vs logs activity.

**File 2: v1-integration-task-decomposition.test.ts**
Tests the dependency/subtask endpoints on issue routes. Mount `issueRoutes` on express. Mock `issueService` (from `../services/index.js`) with: getSubtasks (returns array), createSubtask (returns mock subtask), addDependency (returns mock), removeDependency (returns void), getDependencies (returns array). Also mock `logActivity`.

Test at minimum:
- POST /api/companies/company-1/issues/:issueId/dependencies with valid body returns 201 (or 200)
- GET /api/companies/company-1/issues/:issueId/subtasks returns 200 with array (if endpoint exists)

Before writing tests, read `server/src/routes/issues.ts` to find the exact subtask and dependency endpoint paths. The issue routes file is likely large -- focus on the dependency and subtask sections. Check which service methods the routes call and mock exactly those.

**Important for both files:** Always read the actual route file first to confirm exact paths and service methods. Always include `app.use(errorHandler)`. Always mock `logActivity` from both `../services/index.js` and `../services/activity-log.js`.
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && npx vitest run server/src/__tests__/v1-integration-activity-feeds.test.ts server/src/__tests__/v1-integration-task-decomposition.test.ts --reporter=verbose</automated>
  </verify>
  <done>Both integration test files pass. Activity feed list endpoint returns array. Task decomposition dependency endpoint accepts and returns dependency objects.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for skill profiles and code review</name>
  <files>server/src/__tests__/v1-integration-skill-profiles.test.ts, server/src/__tests__/v1-integration-code-review.test.ts</files>
  <action>
Create 2 new integration test files.

**File 1: v1-integration-skill-profiles.test.ts**
Tests skill profile CRUD routes. Mount `skillProfileRoutes` on express. Mock `skillProfileService` (from `../services/index.js`) with: list (returns array of profiles with id, name, slug, systemPrompt, isBuiltin), getById (returns mock profile), create (returns mock profile), update (returns mock), delete (returns void). Also mock `logActivity`.

Test at minimum:
- GET /api/companies/company-1/skill-profiles returns 200 with array
- POST /api/companies/company-1/skill-profiles with valid body returns 201
- GET /api/companies/company-1/skill-profiles/:id returns 200 with profile object
- DELETE /api/companies/company-1/skill-profiles/:id returns 204 (or 200)

Before writing tests, read `server/src/routes/skill-profiles.ts` to confirm exact paths and service methods. The smoke test in v1-api-smoke.test.ts already tests GET list -- this file adds full CRUD coverage.

**File 2: v1-integration-code-review.test.ts**
Tests code review service functions (service-level, not route test). Import `parsePrUrl` and `buildReviewPayload` from `../services/code-review.js`. These are pure functions that can be tested directly without mocking.

Test at minimum:
- parsePrUrl extracts owner, repo, number from "https://github.com/owner/repo/pull/123"
- parsePrUrl returns null for invalid URLs
- buildReviewPayload returns object with body (string) and comments (array) fields
- buildReviewPayload includes review header in body

Before writing tests, read `server/src/services/code-review.ts` to confirm the exported function signatures and return types. Check if `parsePrUrl` and `buildReviewPayload` are exported. If the main exports are different (e.g., `codeReviewService` factory), test via the factory instead.

Existing tests in `server/src/__tests__/code-review.test.ts` already cover some of these -- read that file first to ensure we add complementary tests, not duplicates. Focus on contract verification (return shapes) rather than duplicating existing logic tests.

**Important:** Always read the actual source file first to confirm exports and signatures. For code review, this is a service-level test (no express/supertest needed).
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && npx vitest run server/src/__tests__/v1-integration-skill-profiles.test.ts server/src/__tests__/v1-integration-code-review.test.ts --reporter=verbose</automated>
  </verify>
  <done>Both integration test files pass. Skill profile CRUD endpoints return correct status codes and shapes. Code review service functions produce expected output structures (parsePrUrl extracts PR metadata, buildReviewPayload returns review body).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run all 4 new files together: `npx vitest run server/src/__tests__/v1-integration-activity-feeds.test.ts server/src/__tests__/v1-integration-task-decomposition.test.ts server/src/__tests__/v1-integration-skill-profiles.test.ts server/src/__tests__/v1-integration-code-review.test.ts --reporter=verbose`
2. Verify no conflicts with plan 01 files (different filenames, no shared mocks)
</verification>

<success_criteria>
- 4 new integration test files exist and pass
- All 8 v1.0 feature areas now have integration test coverage (4 from plan 01 + 4 from this plan)
- Tests follow established supertest + vi.mock pattern (or direct import for service-level tests)
- Tests are permanent regression tests, not temporary verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-ui-full-verification/07-02-SUMMARY.md`
</output>
