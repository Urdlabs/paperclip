---
phase: 07-server-ui-full-verification
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified: []
autonomous: false
requirements:
  - MERGE-02
  - MERGE-03
  - MERGE-04
  - MERGE-05
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03

must_haves:
  truths:
    - "Full test suite passes with zero failures (545+ tests)"
    - "TypeScript compiles with zero errors across all packages"
    - "pnpm build succeeds across all packages"
    - "Docker image builds successfully"
    - "Docker container starts and health endpoint responds"
    - "All MERGE and VERIFY requirements are formally satisfied"
  artifacts: []
  key_links:
    - from: "Dockerfile"
      to: "/api/health"
      via: "Docker HEALTHCHECK + curl"
      pattern: "healthy"
---

<objective>
Run full CI verification suite (typecheck + tests + build), verify Docker build and container health, and formally close all MERGE and VERIFY requirements.

Purpose: This is the gate plan that proves phase 7 is complete. All code changes were made in plans 01 and 02 -- this plan only verifies and validates. Docker verification requires a checkpoint since it needs the Docker daemon.
Output: Verified CI suite, Docker health check confirmation, formal requirement closure.
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-server-ui-full-verification/07-RESEARCH.md
@.planning/phases/06-foundation-database-merge/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full CI verification suite</name>
  <files></files>
  <action>
Run the complete CI verification suite in sequence. This proves MERGE-02 through MERGE-05 and VERIFY-01 through VERIFY-02.

Step 1 -- TypeScript compilation (VERIFY-02, MERGE-02, MERGE-03):
```
pnpm typecheck
```
Expect: zero errors across all packages.

Step 2 -- Full test suite (VERIFY-01):
```
npx vitest run --reporter=verbose
```
Expect: 545+ tests passing, zero failures. The 3 previously-failing timeout tests (fixed in plan 01) must now pass. All 8 new integration test files (from plans 01 and 02) must pass.

Step 3 -- Full build (MERGE-04):
```
pnpm -r build
```
Expect: all packages build successfully. This also validates the pnpm lockfile is correct.

Step 4 -- Lockfile verification (MERGE-04):
```
pnpm install --lockfile-only
```
Expect: completes with no changes to pnpm-lock.yaml.

Step 5 -- Merge commit verification (MERGE-05):
```
git log --oneline 8c83b0d -1
```
Expect: the merge commit from phase 6 exists, confirming all 226 upstream commits are integrated.

If any step fails, investigate and document the failure. Do NOT proceed to the Docker checkpoint if the CI suite has failures -- they must be resolved first.
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && pnpm typecheck && npx vitest run --reporter=verbose && pnpm -r build</automated>
  </verify>
  <done>TypeScript: zero errors. Tests: 545+ passing, zero failures. Build: all packages succeed. Lockfile: clean. Merge commit 8c83b0d exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Docker build and health verification</name>
  <files></files>
  <action>
Claude attempts Docker build and health check automatically for VERIFY-03. If Docker daemon is unavailable, this becomes a manual verification step.

Run these commands in sequence:
1. `docker build -t paperclip:verify .` -- build the Docker image
2. `docker run -d -e PAPERCLIP_EMBEDDED_POSTGRES=true -p 3100:3100 paperclip:verify` -- start container
3. Poll health status every 5 seconds for up to 60 seconds: `docker inspect --format='{{.State.Health.Status}}' $CONTAINER_ID`
4. Verify health endpoint: `curl -sf http://localhost:3100/api/health`
5. Cleanup: `docker stop $CONTAINER_ID && docker rm $CONTAINER_ID`

If Docker is not available, present the commands to the user for manual execution.
  </action>
  <verify>
    <automated>cd /Users/juanjosegongi/dev/urdlabs/paperclip && docker build -t paperclip:verify . && CONTAINER_ID=$(docker run -d -e PAPERCLIP_EMBEDDED_POSTGRES=true -p 3100:3100 paperclip:verify) && sleep 30 && curl -sf http://localhost:3100/api/health && docker stop "$CONTAINER_ID" && docker rm "$CONTAINER_ID"</automated>
  </verify>
  <done>Docker image builds successfully. Container starts and health endpoint at /api/health responds with ok status. VERIFY-03 is satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete, all 8 requirements are formally verified:

| Requirement | Evidence |
|-------------|----------|
| MERGE-02 | `pnpm typecheck` passes -- server and adapter layers compile |
| MERGE-03 | `pnpm -r build` passes -- UI and infrastructure layers compile |
| MERGE-04 | `pnpm install --lockfile-only` produces no changes |
| MERGE-05 | `git log --oneline 8c83b0d -1` confirms merge commit exists |
| VERIFY-01 | `npx vitest run` shows 545+ tests passing, zero failures |
| VERIFY-02 | `pnpm typecheck` shows zero TypeScript errors |
| VERIFY-03 | Docker build succeeds + container health endpoint responds |
| VERIFY-04 | 8 integration test files pass (from plans 01 and 02) |
</verification>

<success_criteria>
- Full CI suite passes: typecheck + tests + build
- 545+ tests passing with zero failures
- Docker image builds and container starts with healthy status
- All 8 phase requirements (MERGE-02 through VERIFY-04) formally verified with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-ui-full-verification/07-03-SUMMARY.md`
</output>
