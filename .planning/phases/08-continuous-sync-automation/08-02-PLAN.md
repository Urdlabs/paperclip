---
phase: 08-continuous-sync-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/sync-status.json
  - README.md
autonomous: true
requirements:
  - SYNC-06

must_haves:
  truths:
    - "README displays a sync health badge in the top badges row"
    - "Badge shows commits-behind count and last sync date"
    - "Badge links to the upstream-sync workflow runs page"
    - "sync-status.json exists with valid shields.io endpoint badge schema"
  artifacts:
    - path: ".github/sync-status.json"
      provides: "Initial badge data for shields.io endpoint badge"
      contains: "schemaVersion"
    - path: "README.md"
      provides: "Sync health badge in badges row"
      contains: "sync-status.json"
  key_links:
    - from: "README.md"
      to: ".github/sync-status.json"
      via: "shields.io endpoint badge URL"
      pattern: "shields.io/endpoint.*sync-status"
    - from: "README.md"
      to: ".github/workflows/upstream-sync.yml"
      via: "badge link href"
      pattern: "actions/workflows/upstream-sync.yml"
---

<objective>
Create the initial sync status badge data file and add the sync health badge to the README.

Purpose: Make the fork's sync status visible at a glance. The shields.io endpoint badge reads from a JSON file in the repo that the upstream-sync workflow updates on each run. The initial JSON seeds the badge with the current state.

Output: `.github/sync-status.json` (new), `README.md` (modified)
</objective>

<execution_context>
@/Users/juanjosegongi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanjosegongi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-continuous-sync-automation/08-CONTEXT.md
@.planning/phases/08-continuous-sync-automation/08-RESEARCH.md

<interfaces>
<!-- README badges row structure (lines 12-16) -->
From README.md:
```html
<p align="center">
  <a href="https://github.com/paperclipai/paperclip/blob/master/LICENSE"><img src="https://img.shields.io/badge/license-MIT-blue" alt="MIT License" /></a>
  <a href="https://github.com/paperclipai/paperclip/stargazers"><img src="https://img.shields.io/github/stars/paperclipai/paperclip?style=flat" alt="Stars" /></a>
  <a href="https://discord.gg/m4HZY7xNG3"><img src="https://img.shields.io/discord/000000000?label=discord" alt="Discord" /></a>
</p>
```

shields.io endpoint badge schema:
```json
{
  "schemaVersion": 1,
  "label": "upstream sync",
  "message": "0 behind | 2026-03-13",
  "color": "brightgreen"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create initial sync-status.json and add README badge</name>
  <files>.github/sync-status.json, README.md</files>
  <action>
**Step 1: Create `.github/sync-status.json`**

Create the `.github/` directory file with the shields.io endpoint badge JSON schema. Set the initial state to reflect that the fork was just synced (Phase 6 merged all 226 upstream commits):

```json
{
  "schemaVersion": 1,
  "label": "upstream sync",
  "message": "0 behind | 2026-03-13",
  "color": "brightgreen"
}
```

This file will be updated by the upstream-sync workflow on each run. The initial value reflects the current state post-Phase-6 merge.

**Step 2: Add badge to README.md**

In the existing badges `<p align="center">` block (around line 12-16 of README.md), add the sync badge AFTER the existing three badges (license, stars, discord). Add it as a new line before the closing `</p>`:

```html
  <a href="https://github.com/Urdlabs/paperclip/actions/workflows/upstream-sync.yml"><img src="https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/Urdlabs/paperclip/master/.github/sync-status.json" alt="Upstream Sync" /></a>
```

IMPORTANT: The badge URL must use:
- `Urdlabs/paperclip` (the fork repo, not upstream `paperclipai/paperclip`)
- `master` branch (default branch where sync-status.json lives)
- The link wraps to the Actions workflow page for the upstream-sync workflow on the fork repo

NOTE: The upstream links in the README header point to `paperclipai/paperclip` (upstream), but the badge must point to `Urdlabs/paperclip` (fork) since that is where the workflow and JSON file live.

Do NOT modify any other part of README.md.
  </action>
  <verify>
    <automated>test -f .github/sync-status.json && python3 -c "import json; d=json.load(open('.github/sync-status.json')); assert d['schemaVersion']==1; assert 'upstream sync'==d['label']; assert 'behind' in d['message']; print('JSON OK')" && grep -q "sync-status.json" README.md && grep -q "Upstream Sync" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  - .github/sync-status.json exists with valid shields.io endpoint badge schema (schemaVersion: 1)
  - JSON contains label "upstream sync", message with behind count and date, color field
  - README.md has sync badge in the badges row alongside license, stars, discord badges
  - Badge image URL points to shields.io endpoint reading from Urdlabs/paperclip master branch
  - Badge link points to upstream-sync workflow runs page on the fork
  </done>
</task>

</tasks>

<verification>
- `.github/sync-status.json` is valid JSON with shields.io endpoint schema
- README.md badges row now has 4 badges (license, stars, discord, upstream sync)
- Badge URL correctly references the fork repo (Urdlabs/paperclip), not upstream
- No other README content modified
</verification>

<success_criteria>
- sync-status.json committed with valid initial badge data
- README shows sync health badge in badges row
- Badge will render on GitHub (shields.io endpoint URL is well-formed)
- Badge links to the upstream-sync workflow page
</success_criteria>

<output>
After completion, create `.planning/phases/08-continuous-sync-automation/08-02-SUMMARY.md`
</output>
